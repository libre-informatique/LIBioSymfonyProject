{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field_value %}
    {% include '@SyliusAdmin/Common/_address.html.twig' with {'address': object.billingAddress} %}
    {% if sm_state(object, 'sylius_order') == 'new' %}
        <div>
            <a href="{{ path('admin_librinfo_crm_address_edit', {'id': object.billingAddress.id}) }}" class="ui icon labeled tiny fluid button openInModal">
                <i class="pencil icon"></i> {{ 'sylius.ui.edit'|trans }}
            </a>
        </div>
    {% endif %}
{% endblock %}
